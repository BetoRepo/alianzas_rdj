<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title><%= title || 'Aula Virtual Scout' %></title>
  <link rel="stylesheet" href="/style.css" />
</head>
<body>
  <header class="site-header">
    <div class="container">
      <a class="brand" href="/">Aula Virtual Scout Venezuela</a>
      <nav class="nav-links">
        <a href="/">Inicio</a>
        <% if (!user) { %>
          <a href="/login">Iniciar sesión</a>
          <a href="/register">Registrarse</a>
        <% } else { %>
          <a href="/dashboard">Mi panel</a>
          <% if (user.is_admin) { %>
            <a href="/admin">Administrador</a>
          <% } %>
          <a href="/logout">Cerrar sesión</a>
        <% } %>
      </nav>
    </div>
  </header>

  <main class="container">
    <% if (message) { %>
      <div class="alert"><%= message %></div>
    <% } %>
    <%- body %>
  </main>

  <footer class="footer">
    <div class="container">
      <p>Asociación Scout de Venezuela · Aula virtual funcional</p>
    </div>
  </footer>
</body>
</html>
